# Required: Discord bot token
# Create a bot at https://discord.com/developers/applications
DISCORD_TOKEN=your_discord_bot_token_here

# Transport mode: "stdio" (default, for Claude Desktop/Code) or "sse" (for claude.ai browser & mobile app)
# When using "sse", the server runs on MCP_PORT and exposes /mcp endpoint
MCP_TRANSPORT=stdio
MCP_PORT=3001

# Required for remote HTTP/SSE mode. Send as Authorization: Bearer <token>.
MCP_AUTH_TOKEN=replace-with-a-long-random-secret

# Optional defaults. Useful on Railway where config.json is not deployed.
DISCORD_GUILD_ID=
ELEVENLABS_VOICE_ID=

# Optional: ElevenLabs API key (only needed if you want send_voice_note to work)
# Voice and model settings are configured in config.json
# Get a key at https://elevenlabs.io
ELEVENLABS_API_KEY=
